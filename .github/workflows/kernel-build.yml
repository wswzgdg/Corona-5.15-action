name: Kernel Build (Reusable)

on:
  workflow_call:
    inputs:
      manager:
        description: 'ROOT manager type'
        required: true
        type: string
      kernel_suffix:
        description: '内核后缀'
        required: false
        type: string
      susfs:
        description: '是否启用 SUSFS'
        required: false
        type: boolean
        default: true
      ccache_update:
        description: '是否覆盖更新 ccache 缓存'
        required: false
        type: boolean
        default: false
      use_kpn:
        description: '是否在编译后使用 KP-N patch 内核镜像'
        required: false
        type: boolean
        default: false
      build_enabled:
        description: '是否执行该 manager'
        required: false
        type: boolean
        default: true
      allow_fail:
        description: '单个管理器失败不算错误'
        required: false
        type: boolean
        default: false

jobs:
  build-kernel:
    if: ${{ inputs.build_enabled }}
    continue-on-error: ${{ inputs.allow_fail }}
    runs-on: ubuntu-latest
    permissions:
      actions: write
      contents: read
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: 配置 ccache
        run: |
          echo "CCACHE_DIR=$HOME/.ccache_5.15" >> $GITHUB_ENV
          echo "CCACHE_MAXSIZE=5G" >> $GITHUB_ENV
          mkdir -p $HOME/.ccache_5.15

      - name: 载入 clang22 缓存
        uses: actions/cache@v5
        with:
          path: ${{ github.workspace }}/clang22
          key: clang22-llvm-22.1.0-${{ runner.os }}
          restore-keys: |
            clang22-llvm-22.1.0-${{ runner.os }}

      - name: 载入 repo 缓存
        uses: actions/cache/restore@v5
        id: repo-restore
        with:
          path: ${{ github.workspace }}/kernel_workspace/.repo
          key: repo-${{ runner.os }}-${{ github.ref_name }}
          restore-keys: |
            repo-${{ runner.os }}-

      - name: 载入 ccache 缓存
        uses: actions/cache/restore@v5
        id: ccache-restore
        with:
          path: ${{ env.CCACHE_DIR }}
          key: ccache-5.15-${{ runner.os }}-${{ github.ref_name }}-${{ inputs.manager }}-${{ inputs.susfs && 'on' || 'off' }}
          restore-keys: |
            ccache-5.15-${{ runner.os }}-${{ github.ref_name }}-${{ inputs.manager }}-
            ccache-5.15-${{ runner.os }}-${{ github.ref_name }}-
            ccache-5.15-${{ runner.os }}-

      - name: 清理 Runner 磁盘空间
        run: |
         mkdir -p /home/runner/.ccache_5.15
         touch /home/runner/.ccache_5.15/DO_NOT_DELETE
         sudo rm -rf /usr/share/dotnet || true
         sudo rm -rf /usr/local/lib/android || true
         sudo rm -rf /opt/ghc || true
         sudo rm -rf /opt/hostedtoolcache/CodeQL || true
         sudo rm -rf /usr/local/share/powershell || true
         sudo rm -rf /usr/local/graalvm || true
         sudo rm -rf /usr/local/.ghcup || true
         sudo rm -rf /usr/local/lib/node_modules || true

         # 清理 APT 缓存和日志
         sudo apt clean
         sudo apt autoclean
         sudo rm -rf /var/log/*.log
         sudo rm -rf /var/cache/apt/archives/*

         #清理临时文件
         sudo rm -rf /tmp/*
         sudo rm -rf /var/tmp/*

         # 查看清理后的空间
         if [ -d "/home/runner/.ccache_5.15" ]; then
         echo "ccache缓存目录存在"
         fi
         df -h
         echo "当前根分区可用空间：$(df -B GB / | grep / | awk '{print $4}')"

      - name: 安装环境依赖
        run: |
          sudo apt update -y
          sudo apt install -y             ccache flex bison dwarves libssl-dev libelf-dev bc python3 python-is-python3             make cmake zip aria2 gnupg gawk rsync             binutils-aarch64-linux-gnu binutils-arm-linux-gnueabihf             tar gzip xz-utils bzip2 device-tree-compiler libc6-dev-i386             repo

      - name: 初始化 ccache
        run: |
          ccache -M ${{ env.CCACHE_MAXSIZE }}
          ccache -o compression=true

      - name: 编译内核
        env:
          KERNEL_COMMON_TOKEN: ${{ secrets.KERNEL_COMMON_TOKEN }}
          AK3_TOKEN: ${{ secrets.AK3_TOKEN }}
          ANDROID_VERSION: android13
          KERNEL_VERSION: '5.15'
          SKIP_APT: '1'
        run: |
          chmod +x .github/workflows/all_managers/common.sh
          .github/workflows/all_managers/common.sh "${{ inputs.manager }}" "${{ inputs.kernel_suffix }}" "${{ inputs.susfs && 'on' || 'off' }}" "${{ inputs.use_kpn && 'true' || 'false' }}"

      - name: 覆盖同 key 的 ccache 缓存
        if: ${{ always() && (inputs.ccache_update || steps.ccache-restore.outputs.cache-hit != 'true') }}
        continue-on-error: true
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CACHE_KEY: ccache-5.15-${{ runner.os }}-${{ github.ref_name }}-${{ inputs.manager }}-${{ inputs.susfs && 'on' || 'off' }}
        run: |
          if gh cache delete "$CACHE_KEY" -R "${{ github.repository }}"; then
            echo "已删除旧的 ccache 缓存: $CACHE_KEY"
          else
            echo "旧的 ccache 缓存不存在，直接保存新缓存"
          fi

      - name: 保存 ccache 缓存
        if: ${{ always() && (inputs.ccache_update || steps.ccache-restore.outputs.cache-hit != 'true') }}
        continue-on-error: true
        uses: actions/cache/save@v5
        with:
          path: ${{ env.CCACHE_DIR }}
          key: ccache-5.15-${{ runner.os }}-${{ github.ref_name }}-${{ inputs.manager }}-${{ inputs.susfs && 'on' || 'off' }}

      - name: 覆盖同 key 的 repo 缓存
        if: ${{ always() && inputs.manager == 'none' }}
        continue-on-error: true
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CACHE_KEY: repo-${{ runner.os }}-${{ github.ref_name }}
        run: |
          if gh cache delete "$CACHE_KEY" -R "${{ github.repository }}"; then
            echo "已删除旧的 repo 缓存: $CACHE_KEY"
          else
            echo "旧的 repo 缓存不存在，直接保存新缓存"
          fi

      - name: 保存 repo 缓存
        if: ${{ always() && inputs.manager == 'none' }}
        continue-on-error: true
        uses: actions/cache/save@v5
        with:
          path: ${{ github.workspace }}/kernel_workspace/.repo
          key: repo-${{ runner.os }}-${{ github.ref_name }}

      - name: 上传 ZIP 工件
        uses: actions/upload-artifact@v7
        with:
          name: ${{ inputs.manager == 'none' && 'ak3-noksu' || format('ak3-{0}', inputs.manager) }}
          path: ${{ github.workspace }}/out_zips/AK3-*.zip
